# ============================================================
# Fonte unica de versao do projeto.
# As constantes abaixo sao reescritas automaticamente pela etapa
# [1.5/6] do build_e_deploy.bat, lendo de versao.json.
# NAO editar manualmente — edite versao.json e rode o build.
# Manter no formato exato (sem indentacao, aspas duplas, em
# linhas separadas) para o regex do .bat funcionar.
# ============================================================
VERSAO = "2.7"
VERSAO_APLICACAO = "v2.7"
